name: Environment Variables

on:
  push

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
       - run: |

           echo "Hello ${{vars.Name}}, how are you.."
           echo "I am ${{vars.age}} years old"
           echo "My password is ${{secrets.pass}}"

